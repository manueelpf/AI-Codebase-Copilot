# AI-Codebase-Copilot
At a high level, the system takes a GitHub repository, processes its code and documentation, stores useful representations of that code, and then answers questions about it through a retrieval pipeline.
